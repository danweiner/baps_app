<%= simple_form_for(pet) do |f| %>  
  <%= f.input :name %>
  <%= f.input :age %>
  <%= f.input :weight, label: 'Weight in lbs' %>
  <%= f.input :breed %>
  <%= f.input :description %>
  <%= f.input :photo, as: :file %>

  <%= f.button :submit, "Submit", class: "btn-primary"%>
<% end %>